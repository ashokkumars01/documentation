# name: Build and Deploy Docs on Release

# on:
#   release:
#     types: [published]

# jobs:
#   build-docs:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'

#       - name: Install Poetry
#         run: curl -sSL https://install.python-poetry.org | python3 -

#       - name: Set PATH
#         run: echo "$HOME/.local/bin" >> $GITHUB_PATH

#       - name: Install dependencies
#         run: poetry install --no-root

#       - name: Get Release Tag
#         id: get_version
#         run: echo "RELEASE_TAG=${{ github.event.release.tag_name }}" >> $GITHUB_ENV

#       - name: Build HTML docs using Poetry + Poe
#         run: |
#           RELEASE_VERSION=${GITHUB_REF#refs/tags/}
#           poetry run poe docs
#           mkdir -p versioned_docs/${RELEASE_VERSION}
#           cp -r output/* versioned_docs/${RELEASE_VERSION}/

#       - name: Deploy to GitHub Pages (gh-pages branch)
#         uses: peaceiris/actions-gh-pages@v4
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: ./versioned_docs
#           # destination_dir: releases


name: Deploy Sphinx Docs to GitHub Pages

on:
  push:
    tags:
      - 'v*.*.*'
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -U pip
          pip install sphinx sphinx-rtd-theme sphinx-multiversion sphinx-design

      - name: Build with sphinx-multiversion
        run: |
          sphinx-multiversion docs build/html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/html


