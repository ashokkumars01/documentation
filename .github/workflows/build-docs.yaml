# name: Build and Deploy Docs on Release

# on:
#   release:
#     types: [published]

# jobs:
#   build-docs:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'

#       - name: Install Poetry
#         run: curl -sSL https://install.python-poetry.org | python3 -

#       - name: Set PATH
#         run: echo "$HOME/.local/bin" >> $GITHUB_PATH

#       - name: Install dependencies
#         run: poetry install --no-root

#       - name: Get Release Tag
#         id: get_version
#         run: echo "RELEASE_TAG=${{ github.event.release.tag_name }}" >> $GITHUB_ENV

#       - name: Build HTML docs using Poetry + Poe
#         run: |
#           RELEASE_VERSION=${GITHUB_REF#refs/tags/}
#           poetry run poe docs
#           mkdir -p versioned_docs/${RELEASE_VERSION}
#           cp -r output/* versioned_docs/${RELEASE_VERSION}/

#       - name: Deploy to GitHub Pages (gh-pages branch)
#         uses: peaceiris/actions-gh-pages@v4
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: ./versioned_docs
#           # destination_dir: releases


name: Deploy Documentation

on:
  push:
    tags:
      - 'v*'        # For versioned docs like v1.0.0
    branches:
      - main        # For latest version

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # required to get tags and history

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build HTML using Sphinx Multiversion
        run: |
          sphinx-multiversion Doc build/html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/html

